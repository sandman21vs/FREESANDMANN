services:
  web:
    build: .
    ports:
      - "4040:8000"
    volumes:
      - freesandmann_data:/app/data
    environment:
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
      - DATABASE_PATH=/app/data/freesandmann.db
      - ADMIN_USERNAME=${ADMIN_USERNAME:-FREE}
    restart: unless-stopped

volumes:
  freesandmann_data:
